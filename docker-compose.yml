version: '3.8'

services:
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins-cicd
    privileged: true
    user: root
    ports:
      - "8080:8080"      # Jenkins Web UI
      - "50000:50000"    # Jenkins Agent port
    volumes:
      # Persist Jenkins data
      - jenkins_home:/var/jenkins_home
      # Mount Docker socket để Jenkins có thể build Docker images
      - /var/run/docker.sock:/var/run/docker.sock
      # Mount Docker binary (cho Windows, có thể cần điều chỉnh)
      - docker-bin:/usr/bin/docker
    environment:
      - JAVA_OPTS=-Djenkins.install.runSetupWizard=false
    networks:
      - jenkins-network
    restart: unless-stopped

volumes:
  jenkins_home:
    driver: local
  docker-bin:
    driver: local

networks:
  jenkins-network:
    driver: bridge
